-- Seed data: default family and ingredient categories
-- This runs automatically on first database init via docker-entrypoint-initdb.d

-- NOTE: Actual table creation is handled by Alembic migrations.
-- This file only seeds initial data after migrations have run.
-- To seed data, run: docker compose exec backend python -m app.seed
